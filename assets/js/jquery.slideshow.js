jQuery.noConflict()(function($){

/* ===============================================
   Slick slideshow
   ============================================= */

	$('.slick-wrapper').slick({
		adaptiveHeight: true,
		fade: true,
		arrows:false,
	});

});          